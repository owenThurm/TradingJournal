import { Progress } from 'antd';

const Winrate = (props) => {

    return (
        <div>
            <Progress type="circle" percent={70} width={60} />
        </div>
    );

}

export default Winrate;